# Make blueprints

Snapshots of the Make scenarios that write into the `Masterlist` Airtable base
(`appqJeEPUeOPaQC3y`), kept in git so changes are reviewable.

| File | Scenario | ID |
|---|---|---|
| `9550684-ghl-status-to-airtable.current.json` | [BeFound] GHL Status → Airtable (Tracy) | 9550684 |

`*.current.json` is an as-is export taken before any change. Re-export after
editing in Make so the snapshot stays truthful.
